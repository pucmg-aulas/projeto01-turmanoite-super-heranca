public class Main {
    // Método principal que serve como ponto de entrada para o programa
    public static void main(String[] args) {
        // Cria uma instância da classe Menu
        Menu menu = new Menu();
        
        // Chama o método exibirMenu() para mostrar o menu ao usuário e permitir interações
        menu.exibirMenu();
    }
}
